/* =================================================
=========== SCHEDULES REDUCER ================
================================================= */
const
	meetingsReducerDefaultState = [];
const action = "Work with it";

export default (
	state =
	meetingsReducerDefaultState,
	action) => {
	switch (action) {
		default:
			return state;
	}
};